      Select模型：默认文件描述符数为1024个，通过不断轮询文件描述符集合，来判断是否有fd就绪。每次select都需要把fd集合从用户态拷贝到内核态。
      
      poll模型：链表的形式存储，没有fd数量上限限制。通过遍历整个链表来判断是否有fd就绪
      
      epoll模型：监视的描述符数量不受限制，IO的效率不会随着监视fd的数量的增长而下降，也需要不断轮询就绪链表，一旦有fd就绪会直接回调fd对应的回调函数进行处理
      
      kqueue模型：类似 epoll